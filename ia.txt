Criar um script (comando artisan) escrito em PHP 8.2 usando o framework laravel 12.20 e phpspredsheet 4.4 com o nome

Os arquivos anexos fazem parte de uma aplicação Laravel 12 + Inertia + React.
Preciso criar um novo layout, novo controller e as páginas (arquivos .jsx) necessárias para dar ao usuário a possibilidade de selecionar uma lista de escolas (composta pela junção das tabelas escolas – Model Escola e cidades – Model Cidade ) a partir de filtros para os campos escolas.cidade_id, escolas.dependencia e escolas.zona e marcar nessa lista as escolas que deverão ter o campo escolas.responsavel_censo atualizados com o valor de users.servidor_id

Criar script (Comando Artisan) para selecionar todos os registros da tabela servidores (Model Servidor) em que o campo email não seja nulo e para cada linha encontrada, caso não exista um registro na tabela users com users.servidor_id = servidores.id, criar um registro na tabela users (Model User) da seguinte forma:
users.name = servidores.nome, users.email = servidores.email, users.servidor_id = servidores.id, users.password = servidores.matricula (o valor de servidores.matricula deve ser criptografado de acordo com o padrão do Laravel 12 para senhas)

Tenho a seguinte situação:
1. Aplicação Laravel 12 + Inertia + React + spatie/laravel-permission + laravel/breezer

2. Banco de dados
2.1. users (id, name, email, email_verified_at, password, remember_token, servidor_id, created_at, updated_at) 
2.2. roles (id, name, guard_name, created_at, updated_at)
2.3. model_has_roles (role_id, model_type, model_id)
2.4. permissions (id, name, guard_name, created_at, updated_at)
2.5. model_has_permissions (permission_id, model_type, model_id)
2.6. role_has_permissions (permission_id, role_id)

2.7. cidades (id, nome, estado_id, regional_id, ibge) - Model Cidade
2.8. escolas (id, nome, endereco, bairro, zona, cidade_id, dependencia, situacao, regulamentacao, tipo_localizacao, modalidade, portaria, ano_adesao, email, atualizado, responsavel_censo, alunos_censo_2024, alunos_simaed, created_at, updated_at)
2.9. regionais (id, nome, sigla, servidor_id, regiao_id, created_at, updated_at) - Model Regional
2.10 regioes (id, nome, sigla, servidor_id, created_at, updated_at) - Model Regiao
2.11 estados (id, nome, uf, ibge, pais, ddd, deleted_at, created_at, updated_at) - Model Estado

3. Informações adicionais
3.1. Já preenchi a tabela roles com os valores ((1,Administrador do sistema,admin,2025-07-24 09:07:03,2025-07-24 09:07:03),
(2,Administrador Estadual,state,2025-07-24 09:07:03,2025-07-24 09:07:03),
(3,Administrador Regional,regional,2025-07-24 09:07:03,2025-07-24 09:07:03),
(4,Técnico Censo Escolar,school,2025-07-24 09:07:03,2025-07-24 09:07:03))
3.2. a tabela users já está preenchida com 14 usuários com ids que vão de 1 14
3.3. o usuário de id igual a 1 deve ser o Administrador do Sistema com acesso à todas as funcionalidades, inclusive de gerenciar usuários

4. Perguntas
4.1. Todas as tabelas listadas no item 2 são realmente necessárias para trabalhar com spatie/laravel-permission
4.2. Como devo proceder  para preencher as tabelas necessárias para liberar o acesso do usuário administrador direto no banco de dados? quais registros devem ser incluídos?

Os arquivos anexos fazem parte de um Aplicação Laravel 12 + Inertia + React + spatie/laravel-permission + laravel/breezer
Usando o layout AuthenticatedLayout.jsx e os componentes anexos, 
preciso que voce crie o código necessário para Listar todas as 
Regionais - regionais (id, nome, sigla, servidor_id, regiao_id, created_at, updated_at) - Model Regional
incluido filtro por Servidor e Regiao + CRUD completo para a model Regional 
+ as relações entre Regiao e Regional, necessárias para implemetação do código e filtros

Seguindo o mesmo padrão do CRUD anterior, me ajude a criar um CRUD para Cidades.
tabela cidades (id, nome, estado_id, regional_id, ibge) - Model Cidade
tabela estados (id, nome, uf, ibge, pais, ddd, deleted_at, created_at, updated_at) - Model Estado
tabela regionais (id, nome, sigla, servidor_id, regiao_id, created_at, updated_at) - Model Regional
Na pagina index que deve listar todas as cidades, deve ter filtro para estado e regional
verifique se as models Cidade, Estado e Regional possui os relacionamentos necessários

Os arquivos anexos fazem parte de um Aplicação Laravel 12 + Inertia + React + spatie/laravel-permission + laravel/breezer.
A autorização para alterar o técnico responsável de uma escola segue a seguinte lógica:
1.	Se o usuário tem uma das funções (role): system-admin ou state-admin ele pode alterar o técnico responsável por qualquer escola
2.	Se o usuário tem a função (role) regiao-admin ele pode alterar o técnico responsável por qualquer escola que pertença a uma cidade (escolas.cidade_id) vinculada a uma regional (cidades.regional_id) que seja vinculada à região (regionais.regiao_id) a qual o usuário esteja vinculado (regioes.servidor_id);
3.	Se o usuário tem a função (role) regional-admin ele pode alterar o técnico responsável por qualquer escola que pertença a uma cidade (escolas.cidade_id) vinculada à regional (cidades.regional_id)  a qual o usuário esteja vinculado (regionais.servidor_id);
Com base no descrito acima e usando as informações do banco de dados anexo onde mostra as relações entre as tabelas, ajuste o código do arquivo EscolaController.php e Atribuir.jsx para que reflitam a lógica definida;

